1. now, we need controller to get expenses
-- it would accept get expenses filter value
-- and it should use it


-- anyway, we should able have infinite scroll 
-- this means that newly fetched expenses should be appended to the list



-- this also means that filter should include offset and limit 

-- it also means that we should fetch more data once we reach the end of the current screen? 
- or maybe once we reach half of the current list?